spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/librarydb
    username: admin
    password: admin

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8081/realms/library

keycloak:
  url: http://localhost:8081
  realm: library
  client-id: library-backend
  client-secret: eveVHDpDkycDuw7MLTwHNRuxBLUlM8u1

server:
  port: 8080